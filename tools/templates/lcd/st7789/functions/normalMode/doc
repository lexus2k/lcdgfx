    /**
     * Switches display to normal mode (sends INVOFF command).
     * @warning Most ST7789 panels initialize with INVON for correct colors.
     * Use this only if your specific panel shows inverted colors with the default.
     * @see invertMode()
     */
