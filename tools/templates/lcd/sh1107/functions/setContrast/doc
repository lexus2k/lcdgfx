    /**
     * Sets display contrast (brightness level).
     * Higher values produce brighter output.
     * @param contrast contrast value (0–255), refer to ~CONTROLLER~ datasheet
     * @see displayOn()
     */
